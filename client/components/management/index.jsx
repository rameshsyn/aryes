import React from 'react'
import {
  Container
} from 'semantic-ui-react'

const Management = ({children}) => {
  return (
    <Container>
      {children}
    </Container>
  )
}

export default Management
