import React from 'react'
import {
  Container
} from 'semantic-ui-react'

const Setting = ({children}) => {
  return (
    <Container>
      {children}
    </Container>
  )
}

export default Setting
