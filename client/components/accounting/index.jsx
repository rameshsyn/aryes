import React from 'react'
import {
  Container
} from 'semantic-ui-react'

const Accounting = ({children}) => {
  return (
    <Container>
      {children}
    </Container>
  )
}

export default Accounting
